import {
  HoverCard,
  HoverCardTrigger,
  HoverCardContent,
} from "@/components/ui/hover-card";

/**
 * Bloom Hover Card - Re-export wrapper for hover preview card components
 *
 * Components exported:
 * - HoverCard: Root hover card element
 * - HoverCardTrigger: Element that triggers the hover card
 * - HoverCardContent: Content displayed in the hover card
 *
 * This is a wrapper layer that will be updated to import from @isanat/bloom-elements
 * when the package is published on npm.
 */

export { HoverCard, HoverCardTrigger, HoverCardContent };
