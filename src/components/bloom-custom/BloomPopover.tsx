import {
  Popover,
  PopoverTrigger,
  PopoverContent,
} from "@/components/ui/popover";

/**
 * Bloom Popover - Re-export wrapper for popover overlay components
 *
 * Components exported:
 * - Popover: Root popover element
 * - PopoverTrigger: Element that triggers the popover
 * - PopoverContent: Content displayed in the popover
 *
 * This is a wrapper layer that will be updated to import from @isanat/bloom-elements
 * when the package is published on npm.
 */

export { Popover, PopoverTrigger, PopoverContent };
