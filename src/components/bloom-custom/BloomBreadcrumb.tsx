import {
  Breadcrumb,
  BreadcrumbList,
  BreadcrumbItem,
  BreadcrumbLink,
  BreadcrumbPage,
  BreadcrumbSeparator,
} from "@/components/ui/breadcrumb";

/**
 * Bloom Breadcrumb - Re-export wrapper for breadcrumb navigation components
 *
 * Components exported:
 * - Breadcrumb: Root navigation element
 * - BreadcrumbList: Ordered list container
 * - BreadcrumbItem: Individual breadcrumb item
 * - BreadcrumbLink: Interactive breadcrumb link
 * - BreadcrumbPage: Current page indicator
 * - BreadcrumbSeparator: Visual separator between items
 *
 * This is a wrapper layer that will be updated to import from @isanat/bloom-elements
 * when the package is published on npm.
 */

export {
  Breadcrumb,
  BreadcrumbList,
  BreadcrumbItem,
  BreadcrumbLink,
  BreadcrumbPage,
  BreadcrumbSeparator,
};
